package com.example.parental1.model

data class RoleResponse(
    val success: Boolean,  // Indicates if the operation was successful
    val message: String    // A message that will be returned, like "Parent role selected."
)
